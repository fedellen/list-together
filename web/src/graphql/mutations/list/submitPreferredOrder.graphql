mutation SubmitPreferredOrder($data: PreferredOrderInput!) {
  submitPreferredOrder(data: $data) {
    userToList {
      listId
      itemHistory {
        id
        removalRating
      }
    }
    errors {
      ...fieldError
    }
  }
}
