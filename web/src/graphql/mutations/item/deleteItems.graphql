mutation DeleteItems($data: DeleteItemsInput!) {
  deleteItems(data: $data) {
    userToList {
      listId
      sortedItems
      list {
        id
        items {
          id
        }
      }
    }
    errors {
      ...fieldError
    }
  }
}
