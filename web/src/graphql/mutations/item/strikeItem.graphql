mutation StrikeItem($data: StrikeItemInput!) {
  strikeItem(data: $data) {
    userToList {
      listId
      sortedItems
      list {
        id
        items {
          id
          strike
        }
      }
    }
    errors {
      ...fieldError
    }
  }
}
