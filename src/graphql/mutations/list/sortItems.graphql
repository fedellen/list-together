mutation SortItems($data: StringArrayInput!, $listId: String!) {
  sortItems(data: $data, listId: $listId) {
    userToList {
      ...userListPartial
    }
    errors {
      ...fieldError
    }
  }
}
